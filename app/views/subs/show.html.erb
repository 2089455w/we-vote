<p id="notice"><%= notice %></p>

<p>
  <strong>Title:</strong>
  <%= @sub.title %>
</p>


<h3><%= link_to 'New post', new_post_path(:sub_id => 1) %></h2>


<div>
  <% @sub.posts.each do |p|%>
    <%= p.title %>
    
  <% end %>
    
  
</div>



<%= link_to 'Edit', edit_sub_path(@sub) %> |
<%= link_to 'Back', subs_path %>
